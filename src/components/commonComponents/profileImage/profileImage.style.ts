import { StyleSheet } from "react-native"

export default StyleSheet.create({
    profileImage: {
        resizeMode: "cover",
        width: "100%",
        height: "100%",
        borderRadius: 100
    }
})